# 0. Print all integers of a list

## Description
This Python function prints all integers in a list. Each integer is printed on a new line using the `str.format()` method. The function assumes the list only contains integers.

## Function Prototype
```python
def print_list_integer(my_list=[]):
