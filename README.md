# Python - More Classes and Objects

This project is part of the **holbertonschool-higher_level_programming** curriculum.  
It focuses on understanding and practicing Python classes, attributes, methods, and object-oriented programming concepts.

## Project: Rectangle Class (0-rectangle.py)

### Task 0: Empty Rectangle Class
Create an empty class `Rectangle` that defines a rectangle.

### Requirements
- No modules can be imported.
- The class should be completely empty (use `pass`).
- File must be executable.

### Example
```python
Rectangle = __import__('0-rectangle').Rectangle

my_rectangle = Rectangle()
print(type(my_rectangle))
print(my_rectangle.__dict__)
