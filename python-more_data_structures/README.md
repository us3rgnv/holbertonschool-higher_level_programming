# 0-square_matrix_simple.py

## Description
This script defines a function `square_matrix_simple` that computes the square of all integers in a 2-dimensional matrix.  
It returns a new matrix with each element squared, while keeping the original matrix unchanged.

## Function Prototype
```python
def square_matrix_simple(matrix=[]):
    # matrix is a 2-dimensional list
    # Returns a new matrix with squared values
